<Window x:Class="AutoSellerUltra.UserWindow.UserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="User profile"
        Height="360" Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,16">
            <Grid Width="52" Height="52" Margin="0,0,14,0">
                <Ellipse Fill="#2B2B2B"/>
                <!-- простой белый силуэт -->
                <Path Fill="White"
                      Data="M16,16 C18.209,16 20,14.209 20,12 C20,9.791 18.209,8 16,8 C13.791,8 12,9.791 12,12 C12,14.209 13.791,16 16,16 Z
                            M16,18 C12.134,18 9,21.134 9,25 L23,25 C23,21.134 19.866,18 16,18 Z"
                      Stretch="Uniform"
                      Width="26" Height="26"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
            </Grid>

            <StackPanel>
                <TextBlock Text="Profile" FontSize="20" FontWeight="Bold"/>
                <TextBlock x:Name="UserLoginHeader" FontSize="14" Opacity="0.85"/>
            </StackPanel>
        </StackPanel>

        <!-- Info table -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="ID:" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="IdText" Margin="0,0,0,8"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Login:" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="LoginText" Margin="0,0,0,8"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Email:" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="EmailText" Margin="0,0,0,8"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Date of birth:" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <TextBlock Grid.Row="3" Grid.Column="1" x:Name="DobText" Margin="0,0,0,8"/>

            <!-- Если в UserDto нет Balance/SpentBalance — удали эти две строки -->
            <TextBlock Grid.Row="4" Grid.Column="0" Text="Balance:" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <TextBlock Grid.Row="4" Grid.Column="1" x:Name="BalanceText" Margin="0,0,0,8"/>

            <TextBlock Grid.Row="5" Grid.Column="0" Text="Spent balance:" FontWeight="SemiBold"/>
            <TextBlock Grid.Row="5" Grid.Column="1" x:Name="SpentText"/>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Close" Width="90" Height="32" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>